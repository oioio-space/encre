# encre

> Une phrase : ce que fait le projet et pour qui.

## Install

```bash
go get github.com/oioio-space/encre
```

## Usage

```go
// Exemple minimal d'utilisation de l'API principale.
```

## Development

```bash
mise run setup   # toolchain + git hooks
mise run ci      # le gate complet (= CI)
```

Voir `CLAUDE.md` pour l'outillage et `PROGRESS.md` pour l'état.
